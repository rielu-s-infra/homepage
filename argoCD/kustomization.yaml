apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rielu-site2
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - imageUpdater.yaml
  - hpa.yaml
  - service-uptimekuma.yaml
  - ingress-uptimekuma.yaml
labels:
  - includeSelectors: true
    pairs:
      app: rielu-site2
